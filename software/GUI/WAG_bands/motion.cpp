#include "motion.h"

Motion::Motion()
{

}

int Motion::getFrameNums() {
    // depends on if we're storing time or frame number
    // if frame number, just get max key
    // if time, need to convert to frames and then return

    return 1000; // TODO fix this
}
