#include "visualization.h"

Visualization::Visualization(GLWidget *displayWidget)
{
    widget = displayWidget;
}


void Visualization::displaySnapshot(PositionSnapshot snapshot) {
    // determines kinematics and then sends center points and orientations to
}

